<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
	<select id="memberSelect" parameterType="smartlock.member.vo.MemberVO"
		resultType="smartlock.member.vo.MemberVO">
		SELECT ID,
		       PASSWORD,
		       NAME,
		       AUTHORITY,
		       PHONE_NUMBER,
		       EMAIL,
		       CORP_ID
		  FROM user 
		 WHERE ID = #{id}
	</select>
	<insert id="memberInsert" parameterType="smartlock.member.vo.MemberVO">
		INSERT INTO user (ID,
		       PASSWORD,
		       NAME,
		       AUTHORITY,
		       PHONE_NUMBER,
		       EMAIL,
		       CORP_ID)
		VALUES (#{id},
		       	#{password},
		       	#{name},
		       	#{authority},
		       	#{phone_number},
		       	#{email},
		       	#{corp_id})
	</insert>
</mapper>